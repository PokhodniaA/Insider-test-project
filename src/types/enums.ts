export enum GameStatus {
  PAUSE = 'pause',
  PLAY = 'play',
  END = 'end',
  NEW = 'new'
}

export enum GameUser {
  USER = 'user',
  COMPUTER = 'computer'
}

export enum Sides {
  LEFT = 'left',
  RIGHT = 'right'
}

export enum ObjectTypes {
  CIRCLE = 'circle',
  TRIANGLE = 'triangle',
  RECTANGLE = 'rectangle',
}

export enum Keyboard {
  ENTER = 'Enter',
  ESC = 'Escape',
  LEFT = 'ArrowLeft',
  RIGHT = 'ArrowRight',
}
