export enum GameStatus {
    PAUSE = 'pause',
    PLAY = 'play',
    END = 'end',
    NEW = 'new'
}

export enum GameUser {
    USER = 'user',
    COMPUTER = 'computer'
}

export enum ObjectTypes {
    CIRCLE = 'circle',
    TRIANGLE = 'triangle',
    RECTANGLE = 'rectangle',
}
